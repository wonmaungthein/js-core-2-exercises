/*
  this function takes an array of persons object that has firstName and lastName
  and returns an array containing name which is concatentation of the two
 */

var getFullNames = function(person) {
  
};

module.exports = getFullNames;